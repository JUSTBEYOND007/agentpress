# Agent Engineering Rules

## Reuse Before Reimplementation

- 优先复用成熟、维护活跃且许可证兼容的开源库、组件和实现；能直接依赖就直接依赖，不能直接依赖时优先复制并做最小必要改造，不重复造轮子。
- 实现前先搜索现有依赖、仓库代码和成熟开源项目，确认没有合适实现后才允许自行开发。
- 复制或改造第三方代码时，必须锁定不可变版本或 commit，保留版权和许可证声明，并在 `THIRD_PARTY_NOTICES.md` 中记录来源、上游路径、本地路径和修改内容。
- 复用代码必须置于 AgentPress 自有接口或适配器之后，不能破坏既定领域模型、持久化事实源、安全边界和公共契约。
- Pi 生态相关实现遵循 `docs/references/pi-ecosystem.md` 的复用矩阵、排除项和验证要求。

## Frontend Reference

- 前端的主要视觉与布局参考为 `docs/references/ui/notion-agent-editor-reference.png`。实现和评审相关页面前必须先查看该原图。
- 工作台采用截图中的桌面三栏结构：左侧工作区导航与内容树、中间无卡片包裹的文章编辑器、右侧可持续对话的 Agent 工作台。
- 参考 Notion 的信息密度、留白、排版层级、细边框、低干扰工具栏和上下文操作方式，并替换为 AgentPress 品牌、领域文案和真实功能。
- 右侧 Agent 工作台需要展示多轮对话、运行步骤、工具结果、文章修改提案和接受/拒绝入口；正文修改仍遵循项目规定的红删绿增 diff，不照搬截图中的蓝色选区表现。
- 不复制截图中的 Notion 商标、账号信息、文章内容或无关功能。截图是桌面端视觉基准，不替代响应式、可访问性和性能要求。

## Commit Discipline

- 每完成一项可独立验收的大功能，完成相应测试和检查后立即创建一次 Git commit；不要把多个无关的大功能堆积在同一提交中。
- 提交标题格式为 `<type>: <简短中文说明>`。
- `type` 仅使用：
  - `feat`：新增功能。
  - `fix`：修复缺陷。
  - `refactor`：不改变外部行为的结构调整。
- 示例：`feat: 完成 Agent 运行状态持久化`。
- 不提交未完成、未验证或混入无关修改的大功能；提交前检查暂存范围，保留用户已有且与当前功能无关的改动。
