# Agent Engineering Rules

## Reuse Before Reimplementation

- 优先复用成熟、维护活跃且许可证兼容的开源库、组件和实现；能直接依赖就直接依赖，不能直接依赖时优先复制并做最小必要改造，不重复造轮子。
- 实现前先搜索现有依赖、仓库代码和成熟开源项目，确认没有合适实现后才允许自行开发。
- 复制或改造第三方代码时，必须锁定不可变版本或 commit，保留版权和许可证声明，并在 `THIRD_PARTY_NOTICES.md` 中记录来源、上游路径、本地路径和修改内容。
- 复用代码必须置于 AgentPress 自有接口或适配器之后，不能破坏既定领域模型、持久化事实源、安全边界和公共契约。
- Pi 生态相关实现遵循 `docs/references/pi-ecosystem.md` 的复用矩阵、排除项和验证要求。

## Agent Architecture Evidence Gate

- Agent 架构、运行协议、消息投影、上下文装配、规划、路由、工具策略、恢复和多 Agent 协作不得凭主观经验直接设计。实现前必须先核对仓库现有依赖与实现，并调研至少两个仍在维护、真实使用 Pi Agent Core 的开源业务项目；同业务领域的成熟实现优先于通用框架和演示项目。
- 调研必须阅读固定 tag 或 commit 下的源码与测试，不能只依据 README、截图、博客、模型总结或仓库热度。结论必须写入 `docs/references/pi-ecosystem.md`，记录上游仓库、不可变 commit、许可证、具体源码/测试路径、可复用行为、适配边界和采用或拒绝原因，之后才允许进入实现。
- 采用顺序固定为：直接使用兼容的成熟依赖；复制成熟模块及其测试并做最小适配；组合多个已验证的成熟模式；只有在没有可用实现时才允许自研。自研前必须留下候选方案和逐项拒绝理由，证明不是遗漏搜索或偏好自造。
- 复制成熟方案时先复制行为测试，再复制或适配实现；不得只仿照表面 API、提示词或 UI。AgentPress 适配层只负责领域模型、PostgreSQL 事实源、安全边界和公共契约转换，不得借适配之名重新发明上游已经解决的状态机。
- Prompt 只能表达角色、领域语义和模型侧策略，不能作为业务状态机、权限、终态、消息归因、恢复或幂等性的唯一实现。优先采用 Pi 自定义消息与 `convertToLlm`、可组合 runtime policy、持久化时间线/事件投影、结构化工具协议和确定性状态转换等已在业务项目验证的模式。
- Bug 修复不得从单次现象直接推导方案。必须先用持久化事实还原事件链，定位所属层，再对照上游同类实现及其回归测试。禁止关键词名单、仅追加 system prompt、组件层绕过状态源等补丁式修复，除非上游方案本身如此且有行为测试证明。
- Agent 行为变更的验收必须同时包含：上游来源对应的契约测试、AgentPress 领域边界测试、相反语义场景的回归测试，以及使用真实 Pi runtime/目标模型的集成或评测。只断言提示词包含某句话、只使用 faux runtime、或只验证 happy path，不足以证明设计成立。
- 若许可证、上游版本、源码路径、测试证据或领域适配边界无法确认，停止实现并先完成调研；不得用“合理推测”补齐缺失证据。

## Frontend Reference

- 前端的主要视觉与布局参考为 `docs/references/ui/notion-agent-editor-reference.png`。实现和评审相关页面前必须先查看该原图。
- Agent 对话、消息流、composer 和 thread 基础交互必须复用成熟开源的 `assistant-ui@0.15.1`，通过 AgentPress adapter 对接 RunEvent/SSE；不得自行重复实现通用 Agent 聊天组件。
- 自研前端组件仅用于计划、任务、Specialist、工具审批、Evidence/引用、费用、恢复状态和文章 diff 等 AgentPress 特有能力。
- 工作台采用截图中的桌面三栏结构：左侧工作区导航与内容树、中间无卡片包裹的文章编辑器、右侧可持续对话的 Agent 工作台。
- 参考 Notion 的信息密度、留白、排版层级、细边框、低干扰工具栏和上下文操作方式，并替换为 AgentPress 品牌、领域文案和真实功能。
- 右侧 Agent 工作台需要展示多轮对话、运行步骤、工具结果、文章修改提案和接受/拒绝入口；正文修改仍遵循项目规定的红删绿增 diff，不照搬截图中的蓝色选区表现。
- 不复制截图中的 Notion 商标、账号信息、文章内容或无关功能。截图是桌面端视觉基准，不替代响应式、可访问性和性能要求。

## Commit Discipline

- 每完成一项可独立验收的大功能，完成相应测试和检查后立即创建一次 Git commit；不要把多个无关的大功能堆积在同一提交中。
- 提交标题格式为 `<type>: <简短中文说明>`。
- `type` 仅使用：
  - `feat`：新增功能。
  - `fix`：修复缺陷。
  - `refactor`：不改变外部行为的结构调整。
- 示例：`feat: 完成 Agent 运行状态持久化`。
- 不提交未完成、未验证或混入无关修改的大功能；提交前检查暂存范围，保留用户已有且与当前功能无关的改动。
